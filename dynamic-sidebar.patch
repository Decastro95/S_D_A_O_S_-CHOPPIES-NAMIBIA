diff --git a/components/Layout.js b/components/Layout.js
index 1111111..2222222 100644
--- a/components/Layout.js
+++ b/components/Layout.js
@@
 import Link from "next/link";
+import { useEffect, useState } from "react";
+import { supabase } from "../lib/supabase";
 
 export default function Layout({ children }) {
-  const navItems = [
-    { href: "/admin", label: "Admin Dashboard" },
-    { href: "/manager", label: "Manager Dashboard" },
-    { href: "/cashier", label: "Cashier Dashboard" },
-  ];
+  const [role, setRole] = useState(null);
+
+  useEffect(() => {
+    const getRole = async () => {
+      const { data: { user } } = await supabase.auth.getUser();
+      if (!user) return;
+
+      const { data: profile } = await supabase
+        .from("profiles")
+        .select("role")
+        .eq("id", user.id)
+        .single();
+
+      if (profile) setRole(profile.role);
+    };
+    getRole();
+  }, []);
+
+  const navItems = [];
+  if (role === "admin") {
+    navItems.push({ href: "/admin", label: "Admin Dashboard" });
+    navItems.push({ href: "/manager", label: "Manager Dashboard" });
+    navItems.push({ href: "/cashier", label: "Cashier Dashboard" });
+  } else if (role === "manager") {
+    navItems.push({ href: "/manager", label: "Manager Dashboard" });
+    navItems.push({ href: "/cashier", label: "Cashier Dashboard" });
+  } else if (role === "cashier") {
+    navItems.push({ href: "/cashier", label: "Cashier Dashboard" });
+  }
 
   return (
     <div className="min-h-screen flex">
       <aside className="w-64 bg-green-700 text-white p-4">
-        <h2 className="text-xl font-bold mb-6">Choppies</h2>
-        <nav className="flex flex-col gap-2">
-          {navItems.map((item) => (
-            <Link key={item.href} href={item.href}>
-              {item.label}
-            </Link>
-          ))}
-        </nav>
+        <h2 className="text-xl font-bold mb-6">Choppies</h2>
+        <nav className="flex flex-col gap-2">
+          {navItems.map((item) => (
+            <Link
+              key={item.href}
+              href={item.href}
+              className="hover:bg-green-600 rounded px-2 py-1"
+            >
+              {item.label}
+            </Link>
+          ))}
+        </nav>
       </aside>
       <main className="flex-1 bg-gray-50 p-6">{children}</main>
     </div>

